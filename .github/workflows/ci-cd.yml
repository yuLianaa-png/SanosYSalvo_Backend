name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Pending CI/CD configuration
        run: echo "Pipeline pendiente de configuración final con AWS ECR y EC2."